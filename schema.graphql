type Account @entity {
  id: ID!
  account: String!
  totalBalance: BigInt!
  lastUpdated: BigInt!
  tokenSeconds: BigInt!
  completed: Boolean!
  _eventCount: BigInt!
}

type Received @entity {
  id: ID!
  recipient: String!
}

type Fullfilled @entity {
  id: ID!
  user: String!
}
